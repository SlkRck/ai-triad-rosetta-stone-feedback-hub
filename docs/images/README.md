# Screenshots

Drop UI screenshots here. The hub page (`../index.html`) looks for these exact
filenames and shows a labeled placeholder until each file exists:

| Filename | Shows |
|---|---|
| `01-signin.png` | Sign-in screen with "Browse without an account" |
| `02-taxonomy.png` | The three-camp BDI taxonomy view |
| `03-debate.png` | A running or completed debate |
| `04-cruxes.png` | The cruxes / decision-points view |

PNG or JPG both work (keep the `.png` name or update the `<img src>` in
`index.html`). Aim for ~1200px wide. Crop out anything sensitive before
committing, since this repo is public.
